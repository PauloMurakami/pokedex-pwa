import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pwa-service',
  webDir: 'dist/pwa=service',
  bundledWebRuntime: false
};

export default config;
